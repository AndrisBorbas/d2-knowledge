// Manual aliases for armor sets whose community sheet name diverges from the
// name Bungie's manifest actually ships under (e.g. renamed before release).
// Keys must already be normalizeLookupName()-shaped (lowercase, single spaces).
export const ARMOR_SET_NAME_ALIASES: Record<string, string> = {
	"uncaged deviants": "Wayward Psyche",
};
